//진행하면서 가장 작은 요소, 즉 최소값을 선택하고 맨 앞으로 배치하는 걸 반복

const selectionSort = (arr) => {
  //최소값 변수 선언
  let minimum = arr[0];

  // 반복문을 통해 배열의 인덱스 0을 최소값으로 지정하고 다음 값과 비교,
  // 처음값보다 작다면 새로운 최소값, 아니라면 다음과 비교
  // 마지막까지 비교 후 위치 인덱스를 바꿔줌
  // 그 후 배열의 인덱스 1부터 다시 반복
};

let arr = [5, 3, 4, 1, 2];
(1)[(1, 3, 4, 5, 2)];
(2)[(1, 2, 4, 5, 3)];
(3)[(1, 2, 3, 5, 4)];
(4)[(1, 2, 3, 4, 5)];
